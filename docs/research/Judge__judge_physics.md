- **ranking**:
  - Architecture 2 — CUPOLA (Uncertainty-Propagating Optimal-cycle Learning, JAX ROM + triage-first)
  - Architecture 3 — CUPOLA (Pragmatic Optimal-control Loop, 0-D screener + fast fork campaign)
  - Architecture 1 — CUPOLA (Coupled Unbinding/Pyrolysis/Oxidation, MOOSE+NEML2 hierarchical multiscale)
- **scores**:
  -
    - **name**: Architecture 2 — CUPOLA (Uncertainty-Propagating Optimal-cycle Learning, JAX ROM + triage-first)
    - **score**: 6.5
    - **rationale**: Best physics-per-unit-complexity, and the only one whose load-bearing claims survived my own recomputation intact. I reproduced its Frank-Kamenetskii numbers exactly (delta = 9.55 / 239 / 1.70e5 at 1mm-250C / 5mm-250C / 10mm-350C) and its design law t_min = dH_c*rho_b*(V/A)/(h*dT_crit) = 47.7 h to three figures. Crucially, it is the ONLY one of the three that uses delta as a diagnostic rather than imposing delta < 0.88 as a hard path constraint — the other two thereby make their own debinding OCP infeasible (see fatal flaws). It is also the only one with a pre-percolation transport branch (free-volume diffusion through undecomposed polymer below eps_c, with V0 = 0.03-0.10 fitted), which is the physically correct treatment of the eps->0 limit where Kozeny-Carman diverges and where cracking actually happens — the regime Arch 3 demotes and Arch 1 mis-quantifies. Its refusal to fit Olevsky's seven closure constants ('they are continuum-mechanics results, not free fits, and fitting them is how the identifiability problem gets manufactured') is the correct methodological stance and directly opposite to Arch 1's plan to replace analytic closures with phase-field-simulated ones of lower reliability. The SSA-collapse-not-pore-closure deadline is the sharpest copper-specific kinetic insight in the set. The complementarity constraint y_O2*y_H2 <= eps is the mathematically right way to forbid a physically impossible (and explosive) gas over a combustion-catalytic metal. The isokinetic reparameterisation kills a verified -0.9999 lnA-E ridge in one line. And the statement that 'no discrepancy term can absorb a MISSING SOURCE TERM', with the 22,400 mol H2O/m3 example, is the most physically mature sentence in any of the three documents. Deductions: the lumped-T field cannot exhibit the runaway its own 99% chance constraint certifies against; the 'binder pre-exponential has zero leverage' claim is regime-conditional and the optimiser's own pO2 throttling leaves that regime; Cantelli kappa = 4.359 (verified) inflates every margin 2.65x in-loop so the reported optimum is a bound-optimum; and it conflates the Cu/Cu2O phase boundary with the dissolved-oxygen equilibrium that the terminal spec actually needs (13-39x tighter). It shares all four of the set-wide omissions.
  -
    - **name**: Architecture 3 — CUPOLA (Pragmatic Optimal-control Loop, 0-D screener + fast fork campaign)
    - **score**: 5.5
    - **rationale**: Correct ranking of which constraints are actually active, the most decisive experiments, and the only honest mechanism in the set for managing model-form risk. Its Darcy computation is the one that checks out: I get 1.69 bar exact (3.1 bar under its linearisation) at eps = 0.02, d = 16 um, L = 5 mm, against 10-23 MPa green strength — the constraint IS slack there, and its d50 < 6 um re-enablement trigger is directionally right (K falls 16x at 4 um). Forcing the OX-vs-WGS chemical fork into a two-arm furnace campaign in week 3 is the single most scientifically valuable action proposed by any of the three, because that fork is a genuine binary unknown that no amount of modelling resolves and that Arch 1 instead pre-decides on a literature screen. The numeric escalation policy (dT_core-surface > 30 K escalates lumped -> 1-D; anisotropy > 1.5 pp escalates to 3-D) is exactly what a referee demands when you demote a PDE term, and it is absent from Arch 1. Measuring sigma_t(T, alpha) on interrupted-debinding coupons at RT and 100/200/300 C is the only route to the denominator of every mechanical constraint, and Raman D/G speciation of the residual carbon is the only attempt in the set to ask what the 200 ppm spec physically means. Deductions, and they are serious: the Balaguer pointwise eta0 inversion is presented as a measurement but presupposes sigma_s(rho), so it conceals rather than breaks the sigma_s0/eta0 degeneracy — I confirmed numerically that scaling both by 100x leaves the free-sintering trajectory bit-identical; it imposes delta < 0.88, which I show caps the debind at 158-214 C and makes its own P2 infeasible; its headline claim that atmosphere control 'is NOT about protecting the copper' is wrong for deoxidation (13-39x below the phase boundary for a 300-100 ppm target); the LOI-then-reduce precision is overclaimed ~100x with no reduction-completeness criterion; and its v0 card's 350 C / 1% H2 / 4 h reduction dwell is kinetically ineffective by its own Ea = 114.6 kJ/mol-plus-incubation model, so the oxide survives into the 800 C carbon hold.
  -
    - **name**: Architecture 1 — CUPOLA (Coupled Unbinding/Pyrolysis/Oxidation, MOOSE+NEML2 hierarchical multiscale)
    - **score**: 4.5
    - **rationale**: The richest equation set and the single sharpest piece of physics in the whole set — but it is the one a hostile referee would dismantle fastest, because its most emphatic 'I verified this' correction is wrong by four orders of magnitude. Genuine strengths: the identifiability seam is correct and I confirmed it numerically — tr(eps_dot) is exactly phi-invariant (identical to 7 digits across phi = 0.5/0.85/1.2), so density-only data from free sintering AND sinter-forging is structurally blind to the shear parameter, and only the ratio sigma_s0/eta0 is identifiable; mandating a deviatoric measurement and CI-failing any standalone reported sigma_s0 is the right response and is enforcement the others lack. The interior-point event formulation of the ordering constraints ([C], [O], chi_red at t_close(x), where t_close is the first crossing of rho_close) is the mathematically correct statement of the problem and is better than Arch 2's terminal/integral form. Insisting on JMAK-with-induction rather than first-order Arrhenius for Cu2O reduction is correct and materially changes the blistering risk. The exhaust-gas observer (zirconia pO2 + dew point + NDIR CO2) is the best instrumentation idea anywhere in the set. Its oxidation ledger arithmetic is exact (I reproduce 5.00 mol O/kg Cu, 112 L STP/kg, 63.5% Cu2O, +43.3 vol%, +12.74% linear) and its sigma_s form ratios (132/361/2401) are self-consistent. But: the 269 bar Darcy claim requires a gas generation rate of 9.9e4 kg/m3/s against a 550 kg/m3 total binder inventory — complete burnout in 5.5 ms; the correct value at eps = 0.15 is 0.039 bar, and this wrong number is elevated to a top-3 risk, a path constraint c5, and a public correction of the brief. It also imposes delta < 0.88, infeasible as shown. Its 4-month L1 applies dry-granular DEM to a 50 vol%, 11.8 Pa.s paste (a category error), to produce K_z/K_xy for the constraint that is slack by three orders. Its multiscale handoff freezes K_ij, phi, psi, sigma_s and Q_s at a pristine RVE the material never occupies (its own +43 vol% oxide eigenstrain reorganises the pore network; adsorbed O and C change gamma_s by >2x). It pre-commits the chemistry to the oxidative route on one external screen, while itself warning that no published copper VPP study contained a dye. Internal inconsistencies compound the impression: 137 W vs 684 W for the same air exotherm (my value: 655 W), and 552 part-volumes of steam quoted at STP inside a constraint-severity argument evaluated at 800-1050 C (correct: ~1970-2430). 26 months before a part, with no early experimental test of the chemical fork.
- **fatal_flaws**:
  -
    - **architecture**: Architecture 1 — CUPOLA (MOOSE+NEML2 multiscale)
    - **flaw**: THE PRESSURE CONSTRAINT c5 IS WRONG BY ~4 ORDERS OF MAGNITUDE AND IS LOAD-BEARING. Quasi-steady compressible Darcy in a symmetric slab gives P_c^2 - P_amb^2 = mu*R*T*mdot*L^2/(K*M) with Kozeny-Carman K = eps^3*d^2/(180(1-eps)^2). At eps = 0.15, d = 16 um: K = 6.64e-15 m^2. A 550 kg/m3 binder inventory burning over a 100 C window at 0.4 C/min gives mdot ~ 0.11 kg/m3/s and a centre overpressure of 0.039 bar — not 269 bar. To reach 269 bar you need mdot = 9.9e4 kg/m3/s, i.e. the entire binder gasified in 5.5 milliseconds (7.4e2 kg/m3/s, 0.74 s, even under their own linearisation). Their 269 -> 22.8 -> 4.2 bar sequence scales exactly as 1/K (ratio 11.80 = K(0.30)/K(0.15) to 4 figures), confirming they propagated the small-perturbation form dP ∝ 1/K into a regime where dP >> P. They then declare 'CONTRARY TO THE BRIEF, this is NOT reliably slack', promote it to a path constraint against a 100-230 bar green strength, and cite it as one of three headline risks. At 269 bar the constraint binds everywhere in P2 and would dominate the entire optimum. A referee who checks one number and finds 10^4 discards the paper.
  -
    - **architecture**: Architecture 1 — CUPOLA (MOOSE+NEML2 multiscale)
    - **flaw**: delta < 0.88 AS A HARD PATH CONSTRAINT MAKES THE OXIDATIVE DEBIND INFEASIBLE. Constraint c1 imposes both T_part - T_furnace <= dT_crit AND Frank-Kamenetskii delta < 0.88. Solving delta(T) = 0.88 with the standard acrylate parameters (Q = 25 MJ/kg, rho_b = 550, E = 150 kJ/mol, A = 1e13, k_eff = 1 W/mK) gives T_crit = 214 C at L = 1 mm, 173 C at 5 mm, 158 C at 10 mm. Nothing decomposes at 160-215 C, so the constraint is mutually infeasible with the terminal binder-removal requirement and the NLP returns infeasible or pins the whole of P2 below 200 C. The error is conceptual: FK criticality is derived for a NON-DEPLETING, zeroth-order, kinetically-controlled reactant with no oxidant limitation. In a pO2-throttled burn — which is this architecture's own prediction (ii) — it is not a failure criterion at all, and the correct criterion is the transient excess temperature, which c1 already contains.
  -
    - **architecture**: Architecture 1 — CUPOLA (MOOSE+NEML2 multiscale)
    - **flaw**: L1(a) IS A CATEGORY ERROR: DRY-GRANULAR DEM APPLIED TO A 50 vol%, 11.8 Pa.s PASTE. Doctor-blade recoating of an LCM slurry is a concentrated-suspension problem governed by lubrication forces, yield stress and cure shrinkage, not by dry granular contact mechanics — LAMMPS-granular/Yade have no representation of the continuous phase at all at phi = 0.5. Worse, the interlayer defect the layer is built to reproduce originates in photopolymerisation shrinkage and oxygen inhibition at the cured layer interface, which the spreading simulation does not contain. Four months and a GPU box to produce K_z/K_xy — for constraint c5, which the calculation above shows is slack by ~3 orders in the open-porosity regime.
  -
    - **architecture**: Architecture 1 — CUPOLA (MOOSE+NEML2 multiscale)
    - **flaw**: THE MULTISCALE HANDOFF IS FROZEN AT A MICROSTRUCTURE THE MATERIAL NEVER OCCUPIES. K_ij(eps), phi(theta), psi(theta), sigma_s(rho) and Q_s are computed once on a pristine RVE and emitted as NEML2 interpolation tables ('a tabulated constitutive handoff, not a live two-way solve'). Every one of these is a strong function of the state the part actually passes through: the architecture's own +43.3 vol% Cu2O eigenstrain reorganises the pore network and changes K by orders of magnitude, and adsorbed oxygen and carbon — precisely the species being removed — change gamma_s and hence sigma_s by more than 2x. The phase-field route to sigma_s also does not close the identifiability gap it is justified by: it replaces an unknown scalar alpha/r0 with a simulated one parameterised by the same unknown surface energies.
  -
    - **architecture**: Architecture 2 — CUPOLA (JAX ROM, triage-first)
    - **flaw**: A LUMPED TEMPERATURE FIELD CANNOT EXHIBIT THE RUNAWAY ITS OWN CHANCE CONSTRAINT CERTIFIES AGAINST. P[max_t (T_part - T_furnace) <= dT_crit] >= 0.99 is evaluated on a model whose temperature is a single lumped state, justified by an EXTERNAL Biot number (2e-4 to 0.67). Biot is the wrong criterion for a body with volumetric generation; the correct one is the internal FK/Damkohler number, which this same document computes as 9.55 to 1.70e5 — supercritical by up to five orders. In the throttled branch the reduction happens to be valid (a 400 W/m2 removal budget across 5 mm at k = 1 W/mK is a 2 K internal gradient), but the constraint's entire purpose is to keep the optimiser OUT of the un-throttled branch, and in that branch the model is structurally blind to the 0.09-12 mm/h burn front it elsewhere asserts exists. You cannot certify a 99% probability against a failure mode your forward model is incapable of producing.
  -
    - **architecture**: Architecture 2 — CUPOLA (JAX ROM, triage-first)
    - **flaw**: THE 'BINDER KINETICS ARE IRRELEVANT' THESIS IS SELF-UNDERMINING. d(ln t_burn)/d(ln A) -> 0 holds only in the oxygen-flux-limited branch. The optimiser's own headline action is to throttle x_O2 from 0.21 toward ~0.002 to cut q_gen (verified: 655 W at air, 31 W at 1% O2, 6.2 W at 0.2% O2, against a 2.8 W removal budget for a 50x50x10 mm part at h = 20, dT = 20 K) — and that is precisely the move that restores kinetic control, where A and E return at full leverage. The triage table that stamps binder kinetics IRRELEVANT is computed on the PRIOR, before the optimiser has selected the operating point that invalidates the regime assumption. The claim is also false for the endothermic char-gasification hold at 700-850 C, which is never FK-supercritical and where the architecture itself admits k_g is unmeasured and uncertain by 10x.
  -
    - **architecture**: Architecture 2 — CUPOLA (JAX ROM, triage-first)
    - **flaw**: CANTELLI TIGHTENING IN-LOOP OPTIMISES A BOUND, NOT THE CONSTRAINT. kappa = sqrt((1-eps)/eps) = 4.359 at eps = 0.05 versus a Gaussian 1.645 — a verified 2.65x inflation of every constraint margin — applied to constraints whose posterior predictive is already sampled and whose empirical quantile is therefore directly available. The reported 'chance-constrained optimum' is the optimum of a distribution-free relaxation and will plausibly be 2-3x longer than the true optimum, which directly undercuts the central publishable claim that the 156 h staircase is provably suboptimal. Related: GATE 0 demands reproduction of two anchor cycles' residual C and O 'within a factor of 2 with NO fitted parameters' while admitting k_g has 10x prior transfer error — a gate that cannot be attributed on either outcome is not a gate.
  -
    - **architecture**: Architecture 3 — CUPOLA (pragmatic loop)
    - **flaw**: THE BALAGUER POINTWISE eta0 INVERSION CONCEALS EXACTLY THE DEGENERACY THE OTHER TWO CORRECTLY IDENTIFY. eta0 = -3*sigma_s(rho)/(18*eps_dot_in*psi(rho)*lambda(rho)) presupposes sigma_s(rho) = (3*alpha/r0)*rho^2. I confirmed numerically that scaling sigma_s0 and eta0 together by 100x leaves the free-sintering trace bit-identical, and that tr(eps_dot) is exactly phi-invariant. So free-sintering dilatometry identifies only the RATIO; fixing sigma_s does not measure eta0, it re-labels an assumption about alpha/r0 — which for a partially oxidised, carbon-contaminated copper surface is uncertain by more than 2x. The architecture ships identifiability.py with FIM eigenvalues and profile likelihood, then publishes 'the copper SOVS material card' from a method that its own tooling should reject. This is the confidently-wrong-published-parameter failure mode, committed by the architecture that has the detector.
  -
    - **architecture**: Architecture 3 — CUPOLA (pragmatic loop)
    - **flaw**: 'ATMOSPHERE CONTROL IS NOT ABOUT PROTECTING THE COPPER' IS WRONG FOR HALF THE PROBLEM, AND IT ALSO IMPOSES THE INFEASIBLE delta < 0.88. The Cu/Cu2O phase boundary governs bulk oxide; the terminal spec O(t_f) <= 300 ppm is a DISSOLVED-oxygen problem. Since a_O is Henrian in [O] and pO2 ~ a_O^2, driving [O] from 3900 ppm (eutectic saturation) to 300 or 100 ppm requires pH2O/pH2 to sit 13x and 39x BELOW the phase boundary — while C3/C6's gasification driving force wants pH2O as high as possible. The tension that Arch 1 correctly names as its central scientific contribution is declared non-existent here, and the carbon hold is optimised as a one-sided problem. Separately, C1 imposes delta < 0.88, which caps the debind at 158-214 C by the same calculation as for Arch 1, making its own P2 infeasible.
  -
    - **architecture**: Architecture 3 — CUPOLA (pragmatic loop)
    - **flaw**: THE LOI-THEN-REDUCE KEYSTONE OVERCLAIMS PRECISION BY ~100x AND HAS NO COMPLETENESS CRITERION. m_binder = m0 - m2 is exact only if the 400 C / 2 h / 4% H2 step reduces the oxide COMPLETELY; the justification extrapolates Ott's NATIVE-oxide reduction (nanometre scale, 350 C, <30 min) to a coupon at ~63% bulk conversion to Cu2O — a 10^4-plus extrapolation in oxide thickness, at pH2 = 0.04 atm, with the water having to escape through a partly sintered skeleton. Residual oxygen biases m2 high and m_binder low, one-sided and undetected. Compounding it, 600 C / 4 h in air on a 16 um powder is well inside the Cu2O surface-sintering regime and can seal porosity and trap carbon — the same sign of error again. The quoted +/-0.005 wt% is balance resolution, not accuracy; realistic accuracy is +/-0.2-0.5 wt% on a 7-14 wt% quantity. The fix is trivial and absent: reduce to constant mass, and run a bare-powder control through the identical LOI+reduce sequence to prove mass return.
  -
    - **architecture**: Architecture 3 — CUPOLA (pragmatic loop)
    - **flaw**: THE v0 CYCLE CARD IS INTERNALLY INCONSISTENT WITH ITS OWN KINETICS. The card oxidises in N2 + 2 vol% O2 up to 450 C, then reduces at 350 C in N2 + 1% H2 for 4 h, then ramps to an 800 C carbon hold. By the architecture's own reduction model (Ea = 114.6 kJ/mol for Cu2O + H2 plus an explicit nucleation incubation tau_inc = tau0*exp(E_inc/RT)), 4 h at 350 C and pH2 = 0.01 atm on a bulk-oxidised body will not reduce a meaningful fraction of the oxide. The oxide therefore survives into the 800 C hold, where it coexists with the char — the exact configuration the missing carbothermic reaction (below) makes violent.
  -
    - **architecture**: ALL THREE
    - **flaw**: THE CARBOTHERMIC CHANNEL Cu2O + C = 2Cu + CO IS MISSING, AND IT IS BOTH THE REAL CARBON SINK AND THE REAL BLISTERING SOURCE. Using the architectures' OWN Ellingham fits — dG(4Cu+O2=2Cu2O) = -333400 + 141.3T and dG(2C+O2=2CO) = -223000 - 175.3T — I get dG(Cu2O + C = 2Cu + CO) = 55200 - 158.3T J/mol, which is negative above 349 K (76 C), with p_CO_eq = 1.5e2 atm at 200 C, 9.7e3 atm at 400 C and 3.8e5 atm at 800 C. Carbon and copper oxide cannot coexist above ~76 C; this is why copper is smelted with carbon. Yet all three model oxide reduction as an H2-only JMAK step (Ea = 114.6 kJ/mol) and carbon removal only as O2 combustion plus C + H2O gasification. In every oxidise-then-reduce route — Arch 1's core bet, Arch 3's OX arm, Arch 2's phases 2->4 — residual char and Cu2O sit in the same state vector from ~250 C onward. Consequences: (a) Arch 1's 'carbon gap' thesis, that carbon removal must wait for K(C+H2O) > 1 at ~660 C (I confirm the crossover at 673 C), is wrong for an air-debound part, whose internal oxide gasifies the carbon at 400 C; (b) CO is insoluble in copper, so this reaction generates high-pressure gas inside a body whose porosity is closing — the classic copper 'gassing' defect; (c) every chance constraint on c_C at t_closure is evaluated with the wrong sink term, and Arch 2's own warning that no discrepancy term can absorb a missing source term applies to all three.
  -
    - **architecture**: ALL THREE
    - **flaw**: THE EUTECTIC CEILING THRESHOLD IS NON-CONSERVATIVE BY ~40x. All three write the constraint as 'T <= 1066 C whenever local [O] > 0.39 wt%', and Arch 1 and Arch 2 then relax toward 1075 C. But 0.39 wt% is the Cu-Cu2O EUTECTIC COMPOSITION, not the threshold for liquid formation. The solid solubility of oxygen in copper at 1066 C is ~0.008 wt% (~80 ppm), so any part above ~100 ppm O begins to melt at the eutectic temperature. Against terminal specs of 200-3000 ppm O, the correct ceiling is 1066 C essentially always until deoxidation is complete — which removes the 1066-1085 C window where copper densification is fastest and materially rewrites P5. Arch 1 is hit hardest (P5 runs 950-1075 C); Arch 3's 1040-1050 C cap is safe only by accident.
  -
    - **architecture**: ALL THREE
    - **flaw**: NO PORE-GAS BACK-PRESSURE IN THE SINTERING STRESS, AND THE 200 ppm CARBON SPEC HAS NO MECHANISM. The effective driving force in the closed-pore regime is sigma_s,eff = 2*gamma/r_pore - p_gas: at gamma = 1.5 J/m2, only 30 bar of entrapped gas at r = 1 um (15 bar at 2 um, 60 bar at 0.5 um) halts densification entirely. The closed-pore gas in an air-debound copper part is H2O/CO/CO2 — all insoluble in Cu, unlike H2, which is why hydrogen sintering works at all. This is the standard terminal-density limiter in precisely the 92->99% window all three name as the target, and none of them has it — Arch 1 builds an entire Phase-0 gate around the sigma_s prefactor while omitting the term that actually caps terminal density. Relatedly, 200 ppm C on a 16 um Cu powder (S_v = 0.042 m2/g) is 6.3 graphene-equivalent (13 chemisorbed) monolayers and the 1550 ppm that 'demonstrably arrests densification' is 49 (103) — both far above monolayer coverage, so surface poisoning cannot be what distinguishes them. The spec is almost certainly about CO/CO2 evolution during densification, i.e. about the missing carbothermic reaction, and none of the three justifies it mechanistically.
  -
    - **architecture**: ALL THREE
    - **flaw**: HYDROGEN EMBRITTLEMENT IS MODELLED AS A DARCY PORE-PRESSURE PROBLEM, WHICH IS THE WRONG MECHANISM AND THE WRONG LENGTH SCALE. Hydrogen disease is lattice diffusion of dissolved H to INTERNAL Cu2O inclusions, producing H2O that is insoluble and immobile in copper and nucleates high-pressure voids at grain boundaries — an intraparticle, sub-continuum process. Arch 1's c5/c7 and Arch 3's C5 ('local H2O generation rate must not exceed the Darcy escape rate') describe inter-particle pore transport and will certify parts that embrittle. All three also adopt a 0.2 mass% O limit, roughly 400x the OFHC specification (<5 ppm for hydrogen service), and Arch 3 then sets a terminal spec of 300 ppm that is inconsistent with its own path constraint and with any conductivity target. Finally, all three allow total pressure down to 50 mbar as a control while using Darcy+Klinkenberg: at 50 mbar and 673 K, lambda = 3.06 um and Kn = 0.48 for a 16 um powder — firmly transitional, so the vacuum branch of every optimiser is running on an invalid transport law.
- **best_ideas_to_graft**:
  - FROM ARCH 1 — EXHAUST-GAS ANALYSIS AS THE ONLINE OBSERVER (zirconia pO2 sensor + dew-point meter + NDIR CO2 or small MS on the retort outlet). The single best idea in the set and non-negotiable. It closes the oxygen and carbon ledgers by direct measurement of the BOUNDARY FLUXES, works without knowing the binder at all, directly measures the two quantities the ordering constraints depend on, and is far cheaper and easier to retrofit than slaving furnace temperature to a shrinkage signal. Critically, it is also the only proposed instrument that would DETECT the missing carbothermic CO burst — an unexplained CO spike at 300-500 C under inert or low-pO2 gas is the signature. Model-predictive re-planning of the debind against this signal converts a large chunk of irreducible binder uncertainty into a feedback problem.
  - FROM ARCH 1 — INTERIOR-POINT EVENT CONSTRAINTS ON THE ORDERING, not integral or terminal surrogates. Define t_close(x) as the first time rho(x,t) crosses rho_close and impose [C](x,t_close) <= spec, [O](x,t_close) <= spec, chi_red(x,t_close) = 1 at that event. This is the mathematically correct statement of an ordering requirement, it is what makes the problem genuinely optimal control rather than a curve fit, and it is what gives the optimiser a positive reason to SLOW the approach to 800-900 C. Replace Arch 2's terminal-time chance constraints and Arch 3's smoothed integral penalty with this form, then apply the chance level to the event constraint.
  - FROM ARCH 1 — MANDATORY DEVIATORIC (DUAL-AXIS) SINTERING MEASUREMENT PLUS CI-ENFORCED IDENTIFIABILITY. I confirmed numerically that tr(eps_dot) is exactly phi-invariant and that scaling (sigma_s0, eta0) together leaves the free-sintering trajectory unchanged: density-only data from free sintering AND from sinter-forging is blind to phi, and free sintering gives only the ratio. So a gravity-loaded overhang or a genuine dual-axis loading path is compulsory, and the build must FAIL when a parameter with a near-zero FIM eigenvalue is reported standalone. Arch 2 has the analysis; Arch 1 has the enforcement; take both. Delete Arch 3's Balaguer pointwise eta0 inversion entirely — it presupposes the answer.
  - FROM ARCH 1 — CuSinteringStress WITH ALL THREE sigma_s FORMS SELECTABLE AND A PHASE-0 GATE ON WHICH IS CORRECT. The rho^2 versus (void fraction)^2 distinction is real and the ratios are arithmetically exact (132x at rho = 0.92, 361x at 0.95, 2401x at 0.98), and a form that vanishes at full density kills densification precisely where copper must succeed. Make it a selectable, unit-tested material model with an upstream issue filed, regardless of which library is used — AND add the pore-gas term sigma_s,eff = 2*gamma/r_pore - p_gas that none of the three has.
  - FROM ARCH 1 — A GAS-COMPOSITION STATE VECTOR SO THE ELLINGHAM MARGIN IS EVALUATED ON THE PORE GAS, NOT THE INLET. Copper equilibrates the water-gas shift at the pore surface, so with residual carbon and an 8 wt% oxygen pickup the internal oxygen potential is set by the local C/O/H balance, not by the MFC setpoints. Carrying y_H2, y_H2O, y_CO, y_CO2, y_CH4, y_O2, y_HC and p as states is what makes constraints c2/c3/C6 mean anything. Arch 3's constraint written on the blend is the weakest version of this.
  - FROM ARCH 1 — BOMB CALORIMETRY FOR dH_c, WITH ARCH 2'S CORRECTION THAT ONLY THE OXIDISED FRACTION COUNTS. The winner's entire governing design law t_min = dH_c*rho_b*(V/A)/(h*dT_crit) scales linearly with dH_c, and 25 MJ/kg is currently an unverified assumption. Measure it — but apply it through Arch 2's split, since pO2 sets what fraction of the binder is removed oxidatively (exothermic, heat-budget-limited) versus pyrolytically (athermal, leaves char).
  - FROM ARCH 3 — THE TWO-ARM OX-vs-WGS FORK EXPERIMENT IN WEEK 3. This is the most scientifically valuable single action proposed by anyone. The oxidise-then-reduce versus never-oxidise-and-gasify choice is a genuine binary unknown with no measured rate constants for copper anywhere in the literature; it is resolved by two weeks of furnace time on identical coupons, not by transferring Roumanie's screen from a different (dye-free) binder. Arch 2 triages PARAMETERS but has no mechanism for resolving MODEL-FORM or route forks; graft this directly in front of its GATE 0.
  - FROM ARCH 3 — THE LOI-THEN-REDUCE MASS-BALANCE CLOSURE, WITH THE MISSING GUARDS ADDED. m_binder = m0 - m2 via 600 C air LOI then H2 reduction is binder-agnostic by construction and beats any TGA inference for w_binder. Add the two things it lacks: reduce to CONSTANT MASS rather than a fixed 2 h, and run a bare-recovered-powder control through the identical LOI+reduce sequence to prove mass return. Quote accuracy (+/-0.2-0.5 wt%), not balance resolution.
  - FROM ARCH 3 — docs/escalation_policy.md WITH QUANTITATIVE TRIGGERS. Written numeric rules for when a demoted term comes back: d50 < 6 um re-enables the Darcy branch; measured dT_core-surface > 30 K escalates lumped-T to 1-D conduction; shrinkage anisotropy > 1.5 pp escalates to 3-D. This is how you survive a referee asking 'why did you delete that term?', and it is the exact mechanism the winner needs to defend its lumped thermal field. Add two triggers of my own: P_tot < 200 mbar switches the transport model off Darcy+Klinkenberg (Kn = 0.48 at 50 mbar), and any measured exhaust CO above baseline under inert gas re-enables the carbothermic channel.
  - FROM ARCH 3 — sigma_t(T, alpha) MEASURED ON INTERRUPTED-DEBINDING COUPONS, at RT and 100/200/300 C, in three build orientations with the interlaminar knockdown. It is the denominator of every mechanical path constraint, it does not exist in the literature for any metal-filled photopolymer, and the strength minimum occurs at a partial conversion where the network is broken but the particles are not yet sintered — which only interrupted coupons sample. Also take the Raman D/G speciation of residual carbon on interrupted coupons: since 200 ppm is already 6-13 monolayer equivalents, whether the carbon is graphitic or amorphous is the physically meaningful variable, not the ppm.
  - FROM ARCH 3 — COMPILE THE ANSWER TO A FURNACE SEGMENT PROGRAM WITH THE ACTIVE-CONSTRAINT TIMELINE ATTACHED (CycleCard -> Eurotherm/Nabertherm), and keep every cycle ever run in git with its run report. Making the deliverable executable and auditable is what turns a simulation project into a result, and the active-constraint timeline is what lets an operator and a referee see which physical limit the cycle is riding at each instant.
  - FROM ARCH 3 — THE BINDER-AGNOSTIC WORST-CASE ENVELOPE CYCLE AS A WEEK-1 DELIVERABLE: union of worst cases over the plausible binder family, pointwise-slowest T(t) and pointwise-lowest pO2(t). Honest, defensible as 'worst-case-envelope cycle synthesis under binder uncertainty', unblocks the furnace immediately, and narrows automatically as the state card fills in — with the wall-clock hours saved by each measurement reported, which is how the next experiment gets funded. Pair it with Arch 1's NDA request to Lithoz for four non-disclosing numbers; pride is not a reason to add three weeks.
  - FROM ARCH 2 (KEEP, DO NOT LOSE IN THE MERGE) — the pre-percolation transport branch below eps_c with V0 = 0.03-0.10 fitted rather than zero, since Kozeny-Carman diverges at eps -> 0 and that is where cracking actually happens; the complementarity constraint y_O2*y_H2 <= epsilon relaxed homotopically, which is both the correct physics over a combustion-catalytic metal and a safety constraint; the isokinetic reparameterisation k(T) = exp(kappa - (E/R)(1/T - 1/T_iso)), a one-line fix for a verified -0.9999 lnA-E ridge (note the stated derivation T_iso = E/(R lnA) is wrong — that is the k = 1 temperature; the right centring is the sensitivity-weighted harmonic-mean temperature of the reaction window, which happens to land near the same 610-625 K); Olevsky's seven closure constants held FIXED at their theoretical values; the SSA-collapse clock as the real gasification deadline rather than pore closure at 92%; Kennedy-O'Hagan discrepancy applied to CONSTRAINT MARGINS rather than raw TGA curves; and the gate requiring the identifiability tooling to independently rediscover degeneracies that are known to be there before it is trusted to find ones that are not.
  - PHYSICS THAT BELONGS TO NONE OF THEM AND MUST BE ADDED BEFORE ANY OF THIS IS PUBLISHABLE: (1) the reaction C(s) + Cu2O = 2Cu + CO(g), dG = 55200 - 158.3*T J/mol from their own Ellingham fits, spontaneous above 76 C, with CO insoluble in Cu — add it to the kinetic set and to the pore-gas balance in every route where char and oxide coexist; (2) the eutectic ceiling rewritten against the SOLIDUS (~0.008 wt% O at 1066 C), not the eutectic composition, which makes 1066 C a near-permanent ceiling and rewrites the densification phase; (3) pore-gas back-pressure in the sintering stress with H2 treated as soluble/escaping and H2O/CO/CO2/N2/Ar as insoluble terminal-density limiters; (4) hydrogen embrittlement as lattice H diffusion to internal Cu2O with an insoluble H2O product, not as a Darcy efflux constraint, and a terminal oxygen spec set against conductivity and ductility rather than against the 0.2 wt% swelling number; (5) delta < 0.88 deleted as a hard path constraint everywhere it appears, retained only as a regime diagnostic, with thermal safety carried entirely by the transient excess-temperature constraint.